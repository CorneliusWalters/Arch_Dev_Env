# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Dependencies
/node_modules

# Environment variables
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Build artifacts
/build
/dist
/out

# Generated repository content
repo_contents_*.txt

# OS generated files
.DS_Store
Thumbs.db

# Ignore user-modified dotfiles that are managed by patches.
# The actual patches (*.patch) ARE tracked, but the live config files are not,
# as they will be re-generated from pristine + patch.
# This list prevents local changes to live config files from being accidentally staged.
/home/*/.config/zsh/.zshrc
/home/*/.config/zsh/.p10k.zsh
/home/*/.config/tmux/tmux.conf
/home/*/.config/lsd/config.yaml
/home/*/.config/nvim/init.lua
/home/*/.config/nvim/lua/keymaps.lua
/home/*/.config/nvim/lua/plugins.lua
/home/*/.config/nvim/lua/preferences.lua
/home/*/.config/nvim/.luacheckrc # Even if removed, good to keep here as a "known ignored" if it ever comes back